# 🎮 Red Strong Tetris

لعبة تتريس احترافية مع نظام جوائز ومسابقات - مبنية بـ Node.js

![Version](https://img.shields.io/badge/version-63.0-blue)
![Node](https://img.shields.io/badge/node-18+-green)
![License](https://img.shields.io/badge/license-MIT-yellow)

---

## ✨ الميزات

### 🎮 اللعبة
- تتريس كامل مع نظام SRS للدوران
- Ghost piece (عرض مكان السقوط)
- Hold piece (حفظ قطعة)
- Hard drop & Soft drop
- T-Spin detection
- Combo system
- 4 أوضاع لعب: Classic, Sprint, Marathon, Ultra

### 🎨 التخصيص
- 12 ثيم مختلف (مجاني + مميز + VIP)
- 5 أشكال للمربعات
- تأثيرات بصرية (glow, particles, scanlines)
- وضع ليلي/نهاري

### 🔊 الصوت
- أصوات متعددة لكل حدث
- موسيقى خلفية
- اهتزاز (Haptic feedback)
- تحكم بالصوت

### 🏆 نظام الجوائز
- مسابقات يومية/أسبوعية
- جوائز نقدية
- لوحة المتصدرين
- نظام الإنجازات

### 👨‍💼 لوحة التحكم
- إدارة اللاعبين
- إدارة الجوائز
- تحديد الفائزين
- إعدادات الموقع
- نظام الفروع
- تقارير وإحصائيات

---

## 📁 هيكل المشروع

```
tetris/
├── server.js                 # السيرفر الرئيسي (Express)
├── package.json              # الحزم
├── config/
│   └── database.js          # إعدادات MySQL
├── routes/
│   ├── admin.js             # API لوحة التحكم
│   ├── auth.js              # تسجيل الدخول
│   ├── game.js              # API اللعبة
│   ├── branch.js            # API الفروع
│   └── wheel.js             # عجلة الحظ
├── middleware/
│   └── auth.js              # التحقق من الصلاحيات
├── public/
│   ├── index.html           # الصفحة الرئيسية
│   ├── game.html            # صفحة اللعبة
│   ├── admin.html           # لوحة التحكم
│   ├── check.html           # فحص الفوز
│   ├── branch.html          # صفحة الفرع
│   ├── js/
│   │   ├── game-engine.js   # محرك اللعبة
│   │   ├── game-audio.js    # نظام الصوت
│   │   ├── game-controls.js # التحكم (Touch + Keyboard)
│   │   ├── themes-v3.js     # نظام الثيمات
│   │   ├── achievements.js  # الإنجازات
│   │   └── ...
│   ├── css/
│   │   ├── themes.css       # ستايلات الثيمات
│   │   ├── game.css         # ستايلات اللعبة
│   │   └── ...
│   └── uploads/             # الصور المرفوعة
└── utils/
    └── helpers.js           # دوال مساعدة
```

---

## ⚙️ المتطلبات

- **Node.js** 18 أو أحدث
- **MySQL** 5.7+ أو **MariaDB** 10.3+
- **PM2** (للإنتاج)

---

## 🚀 التثبيت

### 1. نسخ المشروع
```bash
git clone https://github.com/YOUR_USERNAME/tetris.git
cd tetris
```

### 2. تثبيت الحزم
```bash
npm install
```

### 3. إعداد قاعدة البيانات
```sql
CREATE DATABASE tetris_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'tetris_user'@'localhost' IDENTIFIED BY 'YOUR_PASSWORD';
GRANT ALL PRIVILEGES ON tetris_db.* TO 'tetris_user'@'localhost';
FLUSH PRIVILEGES;
```

### 4. تعديل إعدادات الاتصال
عدل ملف `config/database.js`:
```javascript
module.exports = {
    host: 'localhost',
    user: 'tetris_user',
    password: 'YOUR_PASSWORD',
    database: 'tetris_db'
};
```

### 5. تشغيل السيرفر
```bash
# للتطوير
node server.js

# للإنتاج (مع PM2)
pm2 start server.js --name tetris
pm2 save
```

---

## 📱 الوصول

| الصفحة | الرابط |
|--------|--------|
| اللعبة | `http://localhost:3000/game.html` |
| لوحة التحكم | `http://localhost:3000/admin.html` |
| فحص الفوز | `http://localhost:3000/check.html` |

### بيانات الأدمن الافتراضية:
- **اسم المستخدم:** `admin`
- **كلمة المرور:** `admin123`

⚠️ **غيّر كلمة المرور فوراً بعد التثبيت!**

---

## 🎮 طريقة اللعب

### الكيبورد
| المفتاح | الوظيفة |
|---------|---------|
| ← → | تحريك |
| ↓ | نزول بطيء |
| ↑ أو Space | نزول سريع |
| Z | دوران عكسي |
| X | دوران |
| C أو Shift | حفظ القطعة |
| P أو Esc | إيقاف مؤقت |

### الموبايل (Touch)
| الحركة | الوظيفة |
|--------|---------|
| سحب يمين/يسار | تحريك |
| سحب أسفل | نزول بطيء |
| سحب أعلى | نزول سريع |
| ضغط | دوران |
| ضغط مطول | حفظ القطعة |
| ضغط مزدوج | دوران عكسي |

---

## 🎨 الثيمات

### مجانية
- 🎮 كلاسيكي
- ☀️ نهاري

### مميزة (تُفتح بالنقاط)
- 💡 نيون (500 نقطة)
- 👾 ريترو (300 نقطة)
- 🌅 غروب (400 نقطة)
- 🌊 محيط (600 نقطة)
- 🤖 سايبربانك (1000 نقطة)
- 🌿 طبيعة (350 نقطة)
- 🔥 نار (700 نقطة)

### VIP
- 👑 ذهبي (2000 نقطة)
- 💎 الماسي (3000 نقطة)

---

## 📊 API Endpoints

### اللاعبين
```
POST /api/register          # تسجيل لاعب جديد
POST /api/submit-score      # إرسال النتيجة
GET  /api/leaderboard       # المتصدرين
GET  /api/check-winner/:phone  # فحص الفوز
```

### الأدمن
```
POST /api/admin/login       # تسجيل دخول
GET  /api/admin/players     # قائمة اللاعبين
POST /api/admin/select-winner  # تحديد فائز
GET  /api/admin/settings    # الإعدادات
POST /api/admin/settings    # حفظ الإعدادات
```

---

## 🔧 الصيانة

### النسخ الاحتياطي
```bash
# نسخ قاعدة البيانات
mysqldump -u tetris_user -p tetris_db > backup.sql

# استعادة
mysql -u tetris_user -p tetris_db < backup.sql
```

### السجلات
```bash
# مع PM2
pm2 logs tetris
```

### إعادة التشغيل
```bash
pm2 restart tetris
```

---

## 📝 سجل التغييرات

### v63.0 (الحالي)
- إعادة هيكلة كاملة للكود
- محرك لعبة جديد
- نظام صوت محسن
- تحكم touch أفضل
- نظام ثيمات موحد

### v62.0
- إضافة Hold piece
- تحسين الكيبورد

### v61.0
- نظام الثيمات
- أشكال المربعات

### v60.0
- عجلة الحظ
- المهمات اليومية
- النسخ الاحتياطي التلقائي

---

## 🤝 المساهمة

1. Fork المشروع
2. أنشئ branch جديد (`git checkout -b feature/amazing`)
3. Commit التغييرات (`git commit -m 'Add amazing feature'`)
4. Push للـ branch (`git push origin feature/amazing`)
5. افتح Pull Request

---

## 📄 الترخيص

MIT License - راجع ملف [LICENSE](LICENSE)

---

## 👨‍💻 المطور

تم التطوير بـ ❤️

---

## 🆘 الدعم

لأي استفسار أو مشكلة:
- افتح Issue على GitHub
- أو تواصل عبر البريد الإلكتروني
