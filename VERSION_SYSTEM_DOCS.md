# 📦 نظام إدارة الإصدار (Version Management System)

## 📋 نظرة عامة

نظام مركزي آمن لإدارة رقم الإصدار في جميع أنحاء التطبيق. يقرأ الإصدار من `package.json` ويوزعه تلقائياً على جميع الواجهات والأنظمة.

---

## ✨ الميزات

### 🎯 مركزية الإدارة
- **مصدر واحد**: تعديل رقم الإصدار في مكان واحد فقط (`package.json`)
- **تحديث تلقائي**: ينعكس التغيير على جميع الواجهات فوراً
- **لا حاجة للتعديل اليدوي**: توفير الوقت والجهد

### 🔒 الأمان
- **قراءة فقط**: API لا يسمح بتعديل الإصدار
- **عدم كشف معلومات حساسة**: فقط رقم الإصدار واسم التطبيق
- **حماية ضد POST/PUT/DELETE**: API يقبل GET فقط

### 📍 التوزيع الشامل
يظهر رقم الإصدار في:
1. 🎮 **اللعبة** (game.html) - في الـ footer
2. 🔐 **لوحة الأدمن** (ctrl_x7k9m2p4.html) - في الـ sidebar
3. 🖥️ **Desktop** (desktop.html) - في الـ footer
4. 🔌 **API Endpoint** (/api/version) - للاستعلامات البرمجية
5. 🖥️ **Console** - عند بدء السيرفر

---

## 📁 هيكل الملفات

```
tetris/
├── config/
│   └── version.js              # النظام المركزي لإدارة الإصدار
├── routes/
│   └── version.js              # API Endpoint للإصدار
├── server.js                   # تكامل النظام مع السيرفر
├── package.json                # مصدر رقم الإصدار
└── public/
    ├── game.html               # واجهة اللعبة
    ├── ctrl_x7k9m2p4.html     # لوحة الأدمن
    ├── desktop.html            # Desktop App
    └── js/
        └── desktop.js          # سكريبت Desktop
```

---

## 🔧 كيفية الاستخدام

### 1️⃣ تغيير رقم الإصدار

فقط عدّل `package.json`:

```json
{
  "name": "tetris-game",
  "version": "71.0.0",   ← غيّر هنا فقط
  "description": "Red Strong Tetris Game"
}
```

### 2️⃣ أعد تشغيل السيرفر

```bash
npm start
```

ملاحظة: **لا حاجة** لتعديل أي ملف آخر! 🎉

---

## 🔌 استخدام الـ API

### GET /api/version

**الطلب:**
```http
GET /api/version HTTP/1.1
Host: localhost:3000
```

**الاستجابة:**
```json
{
  "success": true,
  "data": {
    "version": "70.0.0",
    "app": "tetris-game",
    "nodeVersion": "v18.17.0",
    "platform": "linux",
    "timestamp": "2025-01-28T09:00:00.000Z"
  }
}
```

### أمثلة على الاستخدام

**JavaScript/Fetch:**
```javascript
const res = await fetch('/api/version');
const data = await res.json();
console.log('الإصدار الحالي:', data.data.version);
```

**cURL:**
```bash
curl http://localhost:3000/api/version
```

**jQuery:**
```javascript
$.get('/api/version', function(data) {
    console.log('الإصدار:', data.data.version);
});
```

---

## 🛡️ الأمان

### ما يتم عرضه ✅
- رقم الإصدار (version)
- اسم التطبيق (app)
- نسخة Node.js
- نظام التشغيل
- التاريخ والوقت

### ما لا يتم عرضه ❌
- Dependencies
- Scripts
- Environment Variables
- Passwords
- API Keys
- Database Config

### الحماية من التعديل

```javascript
// ✅ مسموح
GET /api/version

// ❌ ممنوع
POST /api/version
PUT /api/version
DELETE /api/version
PATCH /api/version
```

---

## 📍 أماكن الظهور

### 1. اللعبة (game.html)
```html
<!-- في الـ footer -->
<div class="home-footer">
    <p>© 2025 Red Strong. جميع الحقوق محفوظة</p>
    <p>الإصدار: <span id="app-version">v70.0.0</span></p>
</div>
```

### 2. لوحة الأدمن (ctrl_x7k9m2p4.html)
```html
<!-- في الـ sidebar -->
<div class="sidebar-header">
    <h2>Red Strong</h2>
    <small id="admin-version">v70.0.0</small>
</div>
```

### 3. Desktop (desktop.html)
```html
<!-- في الـ footer -->
<div class="footer">
    <p>© 2024 Red Strong. جميع الحقوق محفوظة</p>
    <p>الإصدار: <span id="desktop-version">v70.0.0</span></p>
</div>
```

### 4. Console (عند بدء السيرفر)
```
╔════════════════════════════════════════╗
║  🎮 tetris-game                        ║
║  📦 الإصدار: v70.0.0                  ║
║  🚀 Node.js: v18.17.0                  ║
╚════════════════════════════════════════╝

╔════════════════════════════════════════════════════╗
║   ✅ Running on port 3000                          ║
║   🌐 http://localhost:3000                         ║
║   🔐 Admin: /ctrl_x7k9m2p4.html                   ║
║   📊 Health: /api/health                           ║
║   📦 Version: /api/version                         ║
╚════════════════════════════════════════════════════╝
```

### 5. Health Check API
```json
GET /api/health

{
  "status": "healthy",
  "version": "70.0.0",  ← يستخدم النظام
  "timestamp": "2025-01-28T09:00:00.000Z",
  "database": "connected",
  "uptime": 12345
}
```

---

## 🧪 الاختبار

### 1. اختبار الـ API
```bash
# اختبار GET
curl http://localhost:3000/api/version

# اختبار الحماية من POST
curl -X POST http://localhost:3000/api/version
# النتيجة: 405 Method Not Allowed
```

### 2. اختبار الواجهات
1. افتح المتصفح على `http://localhost:3000`
2. شوف الـ footer → يجب أن يظهر الإصدار
3. افتح لوحة الأدمن → شوف الـ sidebar → يجب أن يظهر الإصدار
4. افتح Desktop App → شوف الـ footer → يجب أن يظهر الإصدار

### 3. اختبار Console
```bash
npm start
```
يجب أن ترى رسالة الإصدار عند بدء السيرفر.

---

## ❓ الأسئلة الشائعة

**س: كيف أغيّر رقم الإصدار؟**  
ج: فقط عدّل `version` في `package.json` وأعد تشغيل السيرفر.

**س: هل يمكن تعديل الإصدار عبر الـ API؟**  
ج: لا، الـ API للقراءة فقط (GET) لأسباب أمنية.

**س: ماذا يحدث إذا فشل تحميل الإصدار؟**  
ج: سيظهر `v0.0.0` كإصدار افتراضي وتطبع رسالة خطأ في Console.

**س: هل يمكن إضافة معلومات أخرى للإصدار؟**  
ج: نعم، عدّل `getVersionForAPI()` في `config/version.js`.

**س: هل يعمل النظام بدون إعادة تشغيل السيرفر؟**  
ج: لا، يجب إعادة تشغيل السيرفر لقراءة التحديثات من `package.json`.

---

## 🔄 التحديثات المستقبلية

### أفكار للتطوير
- ✅ إضافة تاريخ الإصدار
- ✅ إضافة نوع الإصدار (stable, beta, alpha)
- ✅ إضافة changelog API
- ✅ إضافة تحديد إذا كان هناك إصدار أحدث
- ✅ إضافة notification للإصدارات الجديدة

---

## 📝 الخلاصة

### الفوائد الرئيسية

✅ **مركزية**: مكان واحد فقط للتعديل  
✅ **أمان**: لا كشف معلومات حساسة  
✅ **سهولة**: تحديث تلقائي لجميع الواجهات  
✅ **احترافية**: نظام موحد ومتسق  
✅ **تتبع**: معرفة الإصدار الفعلي عند المستخدمين

### طريقة التحديث
1. عدّل `package.json`
2. أعد تشغيل السيرفر
3. **انتهى!** 🎉

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. تأكد من أن `config/version.js` موجود
2. تأكد من أن `routes/version.js` موجود
3. تأكد من أن السيرفر يستورد النظام بشكل صحيح
4. افحص Console للأخطاء

---

**تم إنشاء النظام بتاريخ:** 28 يناير 2025  
**الإصدار الحالي:** v70.0.0  
**الحالة:** ✅ جاهز للإنتاج
